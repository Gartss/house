# 手机表格改版检查记录

本次范围：批量核对、精简头部、底部悬浮入口、房源行列表格、备份次级入口。

## 已通过

- TypeScript检查无错误。
- 生产构建完成，主页与房源/图片接口均生成。
- 本地主页与已登录主页均返回HTTP 200。
- `src/app/tests/batch-review.test.cjs`覆盖并通过：默认不勾选；同时保存多行；未选行修改保留；负数金额和无效日期阻止整批处理；全选后草稿清理；关联房源保留原报价和被拒绝的新字段值；草稿价格、日期和关联目标重开后保留。
- README、首版需求、计划表已同步为表格和批量核对方案。

## 尚未验证

- 本次未执行手机浏览器点击或截图检查，真实手机上的横向滚动、悬浮按钮及相册选择仍需体验验证。
- 尚未部署，移动网络可访问性、正式登录与线上存储待后续阶段验证。
- 完整备份恢复功能不在本次回归范围，仅调整入口；完整功能验收仍见MVP计划第5阶段。

## 当前状态

本次实现与批量逻辑检查完成；不代表全部MVP交付完成。开发预览保留运行供查看。

## 主题窄表格与删除增量检查

- 类型检查和生产构建通过；本地主页返回 HTTP 200。
- 原批量核对回归通过：多行提交、未选行修改保存、无效金额和日期拦截、合并时保留报价、草稿字段重开。
- 新增 `state-transition.test.cjs` 通过：显式单套/批量删除；隐式删除、无效删除列表被拒绝；保留房源的历史报价不可移除或改写；追加报价允许。
- 已同步 README、需求和计划文档。
- 接口端到端测试被本地身份认证阻挡（401），没有写入测试数据；因此尚未验证已登录页面的实际点击保存、删除后刷新和弹窗取消。未修改身份认证绕过此限制。
- 未进行手机实屏截图/交互验收。窄表格、固定对比表头和竖向核对已实现响应式样式，真实手机体验仍待验证。
- 删除原图文件不在本次范围；不会影响其他房源共用图片。现有数据未执行删除。
