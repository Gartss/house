# 阿里云 ECS 部署检查记录

日期：2026-09-09

- 部署方案：方案 B，应用、数据库和图片均保存在用户指定的阿里云 ECS。
- 访问地址：`http://39.106.93.99/`；`http://39.106.93.99:8080/` 继续兼容。
- 运行方式：Docker 容器 `house-app`，设置为异常退出或服务器重启后自动恢复。
- 持久化目录：ECS `/opt/house-data` 挂载到容器 `/data`。
- 端口：安全组允许 TCP 80 和 8080；House 容器同时监听这两个公网端口。
- 验证结果：服务器本机与公网均返回 HTTP 200；容器重启次数为 0。
- 账号验证：创建两个隔离测试账号后，两边均得到独立空数据；登录成功。测试账号随后已删除。
- 构建验证：生产构建与 TypeScript 类型检查通过。
- 线上相册导入兼容：公网 HTTP 页面不提供 `crypto.randomUUID()`，浏览器端编号生成已增加兼容实现；覆盖房源、报价、户型、批量审核和保存请求。兼容分支生成 1000 个唯一 UUID、类型检查、生产构建和重新部署后的公网响应均通过。

待验收：用户使用手机移动网络完成注册、登录、截图导入、编辑、照片查看、保存和再次登录检查。正式长期使用前再接入域名与 HTTPS。

## 服务器清理

2026-09-09 经用户授权清理 ECS：删除无用的 Docker 构建缓存、House 失败版本、临时发布包，以及与 House 无关的 `dispute-platform` 前后端、PostgreSQL 容器、数据卷、镜像、源码和部署残留。磁盘占用由 54% 降至 22%。清理后仅保留 `house-app` 容器和当前 House 镜像；`/opt/house-data` 持久化数据保持正常。

## 2026-09-09 界面更新

- 已发布镜像 `house-app:20260909-ui3`，包含待核对页重新上传、实际面积、装修/电梯统一选项及每行保存操作区调整。
- 切换前已备份 `/opt/house-data`；新容器继续挂载原持久化目录，重启策略保持 `unless-stopped`。
- 服务器本机、公网 80 端口和公网 8080 端口均返回 HTTP 200；未登录访问数据接口返回 401。
- 首次发布包携带 macOS 隐藏资源文件导致测试容器无法启动，正式切换前已清除并通过独立端口验证；旧容器在此期间自动恢复，正式数据未受影响。

## 2026-09-09 本次发布

- 本地构建、核心逻辑测试通过后发布镜像 `house-app:20260909-ui4`。
- 发布包已排除 macOS 隐藏文件、本地数据、截图和临时目录；`/opt/house-data` 保持原数据并完成发布前备份。
- ECS 容器正常运行；公网 80、8080 均返回 HTTP 200，未登录访问 `/api/state` 返回 401。

## 2026-09-10 房源导入与小区趋势更新

- 发布镜像：`house-app:20260910-community-trends`，对应代码提交 `7177912`。
- 更新内容：同一房源的多张截图会合并为一条待核对记录；建筑面积增加局部放大重识别与异常值过滤；同一小区的房源对比时展示一条去重后的小区成交价趋势。
- 发布前已通过真实截图冒烟、核心逻辑测试、TypeScript 类型检查和 Node 22 生产构建。
- 发布包已排除截图、数据、依赖缓存、测试文件及 macOS 隐藏文件；服务器构建前再次检查并清除隐藏文件。
- 数据备份：`/opt/house-backups/house-data-before-20260910-community-trends.tar.gz`；新容器继续挂载 `/opt/house-data:/data`，数据目录发布前后均为 9.1 MB。
- 新镜像先在 ECS 独立端口连续运行 30 秒，主页返回 HTTP 200、未登录 `/api/state` 返回 401、容器重启次数为 0，随后才切换正式流量。
- 切换后 ECS 本机与公网 80、8080 均返回 HTTP 200，未登录 `/api/state` 返回 401；容器重启策略为 `unless-stopped`，重启次数为 0。

## 2026-09-10 房源列表界面更新

- 发布镜像：`house-app:20260910-property-list-ui`，对应代码提交 `d1123ed`。
- 更新内容：房源首页增加房源数量，排序改为下拉选择；筛选区改为统一入口并显示已选条件数量，调整列表可视高度与移动端间距。
- 发布前使用 Node 22 完成 6 组核心测试、生产构建和本地生产服务冒烟；首页返回 HTTP 200，未登录 `/api/state` 返回 401。
- 从 Git 提交生成干净构建目录，发布包未包含 Axhub 原型、未跟踪的 `/prototype` 页面、截图、测试、本地依赖、临时文件或 macOS 隐藏文件。
- 数据备份：`/opt/house-backups/house-data-before-20260910-property-list-ui.tar.gz`；新容器继续挂载 `/opt/house-data:/data`，发布前后数据目录均为 11 MB。
- 候选镜像在独立端口完成启动后连续稳定运行 30 秒，主页返回 HTTP 200、未登录 `/api/state` 返回 401、容器重启次数为 0，随后切换正式流量。
- 切换后 ECS 本机与公网 80、8080 均返回 HTTP 200，未登录 `/api/state` 返回 401；容器重启策略为 `unless-stopped`，重启次数为 0。
